version: '2'
services:
    product-postgresql:
        container_name: product-postgresql
        image: postgres:9.5.4
        # volumes:
        #     - ~/volumes/jhipster/product/postgresql/:/var/lib/postgresql/
        environment:
            - POSTGRES_USER=product
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
